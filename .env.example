# =============================================================================
# Scarff — Environment Configuration Example
# =============================================================================
# Copy this file to .env and fill in the values relevant to your setup.
# Lines starting with # are comments and are ignored at runtime.
# All variables are optional unless marked REQUIRED.
#
# Variable resolution order (highest priority first):
#   1. Actual environment variables (shell / CI / Docker)
#   2. Variables in this .env file (loaded by dotenvy at startup)
#   3. Built-in application defaults
#
# This file is safe to commit to version control.
# Your .env file (with real values) should be in .gitignore.
# =============================================================================


# ── Logging & Observability ──────────────────────────────────────────────────

# Tracing filter. Supports the full RUST_LOG directive syntax.
#
# Examples:
#   RUST_LOG=warn                            → warnings and errors only (default)
#   RUST_LOG=info                            → info + warn + error
#   RUST_LOG=scarff=debug                    → debug for CLI only
#   RUST_LOG=scarff=trace,scarff_core=debug  → granular per-crate control
#
# Default when unset: warn
RUST_LOG=warn

# Log output format.
#   pretty  → human-readable, coloured (default for interactive terminals)
#   compact → single-line, minimal noise (good for CI)
#   json    → structured JSON (for Datadog, Grafana Loki, Splunk, etc.)
SCARFF_LOG_FORMAT=pretty

# Include ISO 8601 timestamps in log lines.
# Set to false when your CI system adds its own timestamps.
# Default: true
SCARFF_LOG_TIMESTAMPS=true


# ── Template Resolution ───────────────────────────────────────────────────────

# Override the template directory search path.
# If unset, Scarff searches (stops at first non-empty directory):
#   1. ./templates
#   2. <executable-dir>/templates
#   3. ../templates  (development fallback)
#SCARFF_TEMPLATES_DIR=/path/to/my/custom/templates

# Allow user templates to override built-in templates with the same ID.
# Default: false
SCARFF_ALLOW_TEMPLATE_OVERRIDE=false


# ── Scaffold Defaults ─────────────────────────────────────────────────────────

# Default language when --language is not specified.
# Supported: rust | python | typescript | go
SCARFF_DEFAULT_LANGUAGE=rust

# Default project kind when --kind is not specified.
# Supported: cli | web-backend | web-frontend | fullstack | worker | library
SCARFF_DEFAULT_KIND=cli

# Default architecture when --architecture is not specified.
# Supported: layered | clean | mvc | feature-modular
SCARFF_DEFAULT_ARCHITECTURE=layered

# Default author name injected as {{AUTHOR}} in templates.
# Falls back to git config user.name if unset.
#SCARFF_AUTHOR=Your Name <you@example.com>


# ── Scaffold Behaviour ────────────────────────────────────────────────────────

# Skip interactive confirmation prompts.
# Equivalent to passing --yes on every invocation.
# Default: false
SCARFF_YES=false

# Enable dry-run mode globally — print plan, write nothing.
# Equivalent to passing --dry-run on every command.
# Default: false
SCARFF_DRY_RUN=false

# Allow overwriting an existing project directory.
# Equivalent to --force. Use with care.
# Default: false
SCARFF_FORCE=false


# ── Output & Display ──────────────────────────────────────────────────────────

# Disable ANSI colour codes.
# Also honoured if NO_COLOR is set (https://no-color.org).
#NO_COLOR=1

# Output format for user-facing messages (separate from log format).
#   auto  → human on TTY, plain when piped (default)
#   human → always coloured
#   plain → no colours, no special characters
#   json  → machine-readable JSON
SCARFF_OUTPUT_FORMAT=auto


# ── Configuration File ────────────────────────────────────────────────────────

# Path to the TOML configuration file.
# Default locations (checked in order):
#   Linux:   ~/.config/scarff/config.toml
#   macOS:   ~/Library/Application Support/scarff/config.toml
#   Windows: %APPDATA%\scarff\config.toml
#   Fallback: .scarff.toml (current directory)
#SCARFF_CONFIG_FILE=~/.config/scarff/config.toml


# ── Environment / Runtime Mode ────────────────────────────────────────────────

# Runtime environment.
#   development  → verbose diagnostics, relaxed validation
#   test         → minimal output, deterministic behaviour
#   production   → strict validation, no extra output (default)
SCARFF_ENV=development

# Emit template resolution diagnostics (which paths checked, specificity scores).
# Requires RUST_LOG=scarff_adapters=debug or SCARFF_ENV=development.
# Default: false
SCARFF_DEBUG_TEMPLATES=false


# ── Future / Experimental (not yet implemented) ──────────────────────────────

# Remote template registry URL (post-MVP).
#SCARFF_REGISTRY_URL=https://registry.scarff.dev

# Registry API key — never commit a real value to version control.
#SCARFF_REGISTRY_API_KEY=

# Local template cache directory.
# Default: ~/.cache/scarff/templates
#SCARFF_TEMPLATE_CACHE_DIR=~/.cache/scarff/templates

# Opt-in anonymous telemetry (post-MVP). Only collected when explicitly true.
# Collects: template name, language, project kind, error category.
# Never collects: project names, file paths, or any of your code.
# Default: false
SCARFF_TELEMETRY_ENABLED=false
